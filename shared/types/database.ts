/**
 * Generated Supabase schema types, re-exported for both clients.
 *
 * The generated file stays at `lib/database.types.ts` because `lib/supabase.ts` imports it
 * as `./database.types`, and that file is `never_agent` in `.claude/autonomy-config.yml` —
 * moving the types would force an edit to a closed path for no benefit.
 *
 * Regenerate with the Supabase CLI as before; nothing about that workflow changes.
 */
export type { Database, Json } from '../../lib/database.types';
