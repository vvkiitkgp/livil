# Copy to web/.env.local (gitignored) and fill in.
#
# Both values are the PUBLIC project URL and anon key — the same pair already committed in
# lib/supabase.ts, safe to expose in a browser bundle because every table is protected by
# row-level security. Never put a service-role key here: Vite inlines every VITE_* variable
# into the shipped JavaScript.
VITE_SUPABASE_URL=https://fqzrmqnlgjeuxzinbqvs.supabase.co
VITE_SUPABASE_ANON_KEY=
